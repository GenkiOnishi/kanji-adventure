// Kanji Adventure Configuration
// Offline-first version: no API key required.
// Handwriting recognition uses Kanji Canvas (client-side, fully offline).

// Legacy variables kept for backwards compatibility
const ANTHROPIC_API_KEY = "";  // Not used in offline mode
const OCR_ENABLED = false;     // Using offline recognition
